diff --git a/node_modules/@atomic-editor/editor/.bun-tag-8b5ba6dc9b98f0e3 b/.bun-tag-8b5ba6dc9b98f0e3
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@atomic-editor/editor/.bun-tag-911766f951a9017e b/.bun-tag-911766f951a9017e
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@atomic-editor/editor/.bun-tag-b078cfe0b0463c9b b/.bun-tag-b078cfe0b0463c9b
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@atomic-editor/editor/.bun-tag-fb7ae7c466f3f7e0 b/.bun-tag-fb7ae7c466f3f7e0
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 9e17e82ee7ef8a8a558dbd0444faf2ab2f8836dc..dd472ca33425b6bf1c4ff5b9ca39c5ec044ab4de 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -9,4 +9,5 @@ export { wikiLinks } from './wiki-links';
 export type { WikiLinkResolvedTarget, WikiLinkStatus, WikiLinkSuggestion, WikiLinksConfig, } from './wiki-links';
 export { atomicEditorTheme, atomicMarkdownSyntax } from './atomic-theme';
 export { autoCloseCodeFence, extendEmphasisPair } from './edit-helpers';
+export { treeGrowthEffect, treeProgressPlugin } from './tree-progress';
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index 955ad2f60d19dc8619d5a35fa1778f15c2e1ef15..0577bf5f3093fcff15679fab28a2ef3a534d3269 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -10,4 +10,5 @@ export { tables } from './table-widget';
 export { wikiLinks } from './wiki-links';
 export { atomicEditorTheme, atomicMarkdownSyntax } from './atomic-theme';
 export { autoCloseCodeFence, extendEmphasisPair } from './edit-helpers';
+export { treeGrowthEffect, treeProgressPlugin } from './tree-progress';
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/inline-preview.d.ts b/dist/inline-preview.d.ts
index 4951e264cdf8f9e1c2f52936ed6e4cb6558769e1..f643219c6564a54e18261e644ed9d11da0e730ba 100644
--- a/dist/inline-preview.d.ts
+++ b/dist/inline-preview.d.ts
@@ -8,6 +8,12 @@ export interface InlinePreviewConfig {
      * mechanism.
      */
     onLinkClick?: (url: string) => void;
+    /**
+     * When `true`, every line renders with its markup hidden regardless of the
+     * cursor position — a full reading view (Sunstone VIEW mode). Defaults to
+     * `false`, the Obsidian-style hybrid where the active line shows raw markup.
+     */
+    alwaysRender?: boolean;
 }
 /**
  * Assemble the inline-preview extension set. Call once per editor and
diff --git a/dist/inline-preview.js b/dist/inline-preview.js
index 94d48d196e21fac09a774cd97b61ed5274ab48b9..398840c8a2c5fd19e624099ec20e695b1a6a8df1 100644
--- a/dist/inline-preview.js
+++ b/dist/inline-preview.js
@@ -1,5 +1,5 @@
 import { ensureSyntaxTree, syntaxTree } from '@codemirror/language';
-import { EditorSelection, Prec, StateEffect, StateField, } from '@codemirror/state';
+import { EditorSelection, Facet, Prec, StateEffect, StateField, } from '@codemirror/state';
 import { Decoration, EditorView, ViewPlugin, WidgetType, keymap, } from '@codemirror/view';
 import { treeGrowthEffect, treeProgressPlugin } from './tree-progress';
 function defaultOnLinkClick(url) {
@@ -11,6 +11,13 @@ function defaultOnLinkClick(url) {
         // is fine; the caller can supply an opener that handles this.
     }
 }
+// Sunstone VIEW-mode facet: when any input is `true`, the inline-preview
+// decoration builder treats no line as active, so every line's raw markup
+// hides (renders) regardless of cursor position — a full reading view. The
+// host toggles this by reconfiguring the extension via a Compartment.
+const alwaysRenderFacet = Facet.define({
+    combine: (values) => values.some(Boolean),
+});
 const FREEZE_TAIL_MS = 100;
 // ---- freeze plumbing -----------------------------------------------------
 const setFrozen = StateEffect.define();
@@ -24,13 +31,17 @@ const previewFrozenField = StateField.define({
         return prev;
     },
 });
-function linkIconHitTarget(event, root) {
+function linkIconHitTarget(event, root, wholeLink = false) {
     const target = event.target;
     if (!(target instanceof Element))
         return null;
     const linkEl = target.closest('.cm-atomic-link');
     if (!linkEl || (root && !root.contains(linkEl)))
         return null;
+    // Sunstone VIEW (reading) mode: the link text isn't editable, so the
+    // whole link is the click target — no need to hit the trailing icon.
+    if (wholeLink)
+        return linkEl;
     // The icon is a `::after` pseudo-element, so it doesn't have its own
     // event target. Compute the same trailing hit-zone used by the click
     // opener and treat pointerdown in that zone as "on the icon", not
@@ -88,7 +99,7 @@ const freezeMousePlugin = ViewPlugin.fromClass(class {
                 if (!(target instanceof Node) || !this.view.contentDOM.contains(target)) {
                     return;
                 }
-                if (linkIconHitTarget(event, this.view.contentDOM)) {
+                if (linkIconHitTarget(event, this.view.contentDOM, this.view.state.facet(alwaysRenderFacet))) {
                     // Let the follow-up click open the link, but stop CM6 from
                     // interpreting the icon press as a text-editing click. Without
                     // this, pointerdown moves the selection into the Link node and
@@ -178,6 +189,20 @@ const HIDEABLE_SYNTAX = new Set([
 // The same token names can appear under an Image node — those stay
 // on the line-based rule because images are a different UX surface.
 const LINK_CHILD_SYNTAX = new Set(['LinkMark', 'URL', 'LinkTitle']);
+/**
+ * The `URL` child that is a Link's DESTINATION, or `null` when it has none.
+ *
+ * GFM autolinking also turns a bare URL / email address in the link LABEL into
+ * its own `URL` node, so `[a@b.c](mailto:a@b.c)` has TWO direct `URL` children
+ * and `getChild('URL')` returns the label one. Destination vs label is a
+ * positional question: the label ends at the closing `]` (the Link's second
+ * `LinkMark`), so only a `URL` starting after it is the destination.
+ */
+function destinationUrlNode(link) {
+    const marks = link.getChildren('LinkMark');
+    const labelEnd = marks.length > 1 ? marks[1].to : link.from;
+    return link.getChildren('URL').find((u) => u.from >= labelEnd) ?? null;
+}
 const INLINE_MARK_CLASS = {
     StrongEmphasis: 'cm-atomic-strong',
     Emphasis: 'cm-atomic-em',
@@ -289,7 +314,11 @@ function buildInlineDecorations(view) {
     const { doc } = state;
     const ranges = [];
     const activeLines = new Set();
-    if (view.hasFocus) {
+    // Sunstone VIEW mode: with alwaysRender set, leave activeLines (and the
+    // link-active set below) empty so the line-based reveal rule hides every
+    // line's markup — full reading view independent of the cursor.
+    const alwaysRender = state.facet(alwaysRenderFacet);
+    if (view.hasFocus && !alwaysRender) {
         for (const r of state.selection.ranges) {
             const firstLine = doc.lineAt(r.from).number;
             const lastLine = doc.lineAt(r.to).number;
@@ -322,6 +351,12 @@ function buildInlineDecorations(view) {
     // aren't included; they already have their own widget UX and the
     // line-based reveal is the right fit for `![alt](url)`.
     const activeLinkStarts = new Set();
+    // `from` positions of Link nodes that have NO destination — a bracketed
+    // span like an OKF citation marker `[1]` (or `[note]`) with no matching
+    // reference definition. lezer still parses these as `Link` nodes, but they
+    // aren't links: there's nothing to open. We neither style them nor hide
+    // their brackets, so they render as the literal `[1]` source text.
+    const urllessLinkStarts = new Set();
     // Single pre-order walk. A tree walk visits a parent before its
     // children, which lets us compute two pieces of look-ahead state on
     // the way in — right before the children that depend on them:
@@ -353,14 +388,21 @@ function buildInlineDecorations(view) {
                         activeLines.add(n);
                 }
             }
-            if (node.name === 'Link' && view.hasFocus) {
-                for (const range of state.selection.ranges) {
-                    // Inclusive overlap: cursor sitting exactly on either
-                    // boundary counts as inside, matching the UX where the
-                    // next keystroke affects the link.
-                    if (range.from <= node.to && range.to >= node.from) {
-                        activeLinkStarts.add(node.from);
-                        break;
+            if (node.name === 'Link') {
+                // A Link with no URL child has no destination — treat it as
+                // plain bracketed text (`[1]`), not a link (see urllessLinkStarts).
+                if (!destinationUrlNode(node.node)) {
+                    urllessLinkStarts.add(node.from);
+                }
+                else if (view.hasFocus && !alwaysRender) {
+                    for (const range of state.selection.ranges) {
+                        // Inclusive overlap: cursor sitting exactly on either
+                        // boundary counts as inside, matching the UX where the
+                        // next keystroke affects the link.
+                        if (range.from <= node.to && range.to >= node.from) {
+                            activeLinkStarts.add(node.from);
+                            break;
+                        }
                     }
                 }
             }
@@ -374,7 +416,8 @@ function buildInlineDecorations(view) {
                 }
             }
             const markClass = INLINE_MARK_CLASS[node.name];
-            if (markClass && node.from < node.to) {
+            if (markClass && node.from < node.to &&
+                !(node.name === 'Link' && urllessLinkStarts.has(node.from))) {
                 ranges.push(Decoration.mark({ class: markClass }).range(node.from, node.to));
             }
             if (HIDEABLE_SYNTAX.has(node.name) && node.from < node.to) {
@@ -389,8 +432,27 @@ function buildInlineDecorations(view) {
                     while (parent && parent.name !== 'Link' && parent.name !== 'Image') {
                         parent = parent.parent;
                     }
-                    if (parent && parent.name === 'Link') {
-                        shouldHide = !activeLinkStarts.has(parent.from);
+                    if (parent && parent.name === 'Link' &&
+                        node.name === 'URL' && destinationUrlNode(parent)?.from !== node.from) {
+                        // An autolinked URL / email address in the link LABEL
+                        // (`[a@b.c](mailto:a@b.c)`) is the link's visible TEXT,
+                        // not its destination — hiding it leaves an empty anchor.
+                        shouldHide = false;
+                    }
+                    else if (parent && parent.name === 'Link') {
+                        // Keep the brackets of a url-less Link (`[1]`) visible —
+                        // it renders as literal text, not a hidden-mark link.
+                        shouldHide = !urllessLinkStarts.has(parent.from) &&
+                            !activeLinkStarts.has(parent.from);
+                    }
+                    else if (node.name === 'URL') {
+                        // A bare/GFM-autolinked URL (`https://…` in running
+                        // text) parses as a standalone URL node with no Link
+                        // parent — its text IS the visible content, so hiding it
+                        // would blank the line. Keep it visible and style it as a
+                        // link (the click handler resolves bare URLs too).
+                        shouldHide = false;
+                        ranges.push(Decoration.mark({ class: INLINE_MARK_CLASS.Link }).range(node.from, node.to));
                     }
                     else {
                         shouldHide = !activeLines.has(lineNum);
@@ -788,7 +850,7 @@ function makeLinkClickHandler(onLinkClick) {
                 return false;
             if (event.button !== 0)
                 return false;
-            const linkEl = linkIconHitTarget(event, view.contentDOM);
+            const linkEl = linkIconHitTarget(event, view.contentDOM, view.state.facet(alwaysRenderFacet));
             if (!linkEl)
                 return false;
             const pos = view.posAtDOM(linkEl);
@@ -796,11 +858,19 @@ function makeLinkClickHandler(onLinkClick) {
                 return false;
             const tree = syntaxTree(view.state);
             let node = tree.resolveInner(pos, 1);
-            while (node && node.name !== 'Link')
+            // Bare/autolinked URLs render as a standalone URL node with no Link
+            // parent; resolve those directly. Otherwise walk up to the Link.
+            while (node && node.name !== 'Link' && node.name !== 'URL')
                 node = node.parent;
             if (!node)
                 return false;
-            const urlNode = node.getChild('URL');
+            // A click landing on an autolinked label (`[a@b.c](mailto:a@b.c)`)
+            // resolves to the LABEL's URL node; open the Link's destination, not
+            // the label text.
+            if (node.name === 'URL' && node.parent?.name === 'Link') {
+                node = node.parent;
+            }
+            const urlNode = node.name === 'URL' ? node : destinationUrlNode(node);
             if (!urlNode)
                 return false;
             const url = view.state.doc.sliceString(urlNode.from, urlNode.to);
@@ -821,8 +891,9 @@ function makeLinkClickHandler(onLinkClick) {
  * browser, etc.) instead of the default `window.open`.
  */
 export function inlinePreview(config = {}) {
-    const { onLinkClick = defaultOnLinkClick } = config;
+    const { onLinkClick = defaultOnLinkClick, alwaysRender = false } = config;
     return [
+        alwaysRenderFacet.of(alwaysRender),
         previewFrozenField,
         inlinePreviewPlugin,
         freezeMousePlugin,
diff --git a/dist/styles/inline-preview.css b/dist/styles/inline-preview.css
index de6e2618e9b0bd8c788aea16f95d6a9181f84e67..c7898c0bcb907a5c6ee3f32db2e249bc97042cc5 100644
--- a/dist/styles/inline-preview.css
+++ b/dist/styles/inline-preview.css
@@ -195,6 +195,14 @@
   color: var(--atomic-editor-link-hover, #a5b4fc);
 }
 
+/* Reading view (Sunstone VIEW mode): the whole link follows the link, not
+ * just the trailing icon, so the pointer cursor spans the link text too.
+ * Scoped to the non-editable content DOM — VIEW mode sets
+ * `contenteditable="false"`; edit/hybrid keep the text-caret cursor. */
+.cm-content[contenteditable="false"] .cm-atomic-link {
+  cursor: pointer;
+}
+
 /* External-link icon. In prose it's a `::after` pseudo-element; inside
  * table cells it's a real `.cm-atomic-link-icon` element (a pseudo can't
  * receive a click, so the in-cell icon wouldn't open the link). Both
@@ -457,6 +465,7 @@
 .cm-atomic-table-cell-source .cm-atomic-strong-wrap.active > .cm-atomic-mark,
 .cm-atomic-table-cell-source .cm-atomic-em-wrap.active > .cm-atomic-mark,
 .cm-atomic-table-cell-source .cm-atomic-strike-wrap.active > .cm-atomic-mark,
+.cm-atomic-table-cell-source .cm-atomic-code-wrap.active > .cm-atomic-mark,
 .cm-atomic-table-cell-source .cm-atomic-link-wrap.active > .cm-atomic-mark {
   display: inline;
   color: var(--atomic-editor-fg-faint, #666);
diff --git a/dist/table-widget.d.ts b/dist/table-widget.d.ts
index 1915826e8e0122832658bdd15e8fee8649f5499f..9f10d96ce3b9ca82d668ed5884a20b218d9f4a23 100644
--- a/dist/table-widget.d.ts
+++ b/dist/table-widget.d.ts
@@ -19,6 +19,10 @@ type CellToken = {
 } | {
     type: 'strike';
     children: CellToken[];
+} | {
+    type: 'code';
+    delim: string;
+    text: string;
 } | {
     type: 'link';
     textChildren: CellToken[];
diff --git a/dist/table-widget.js b/dist/table-widget.js
index 55756c13971b3d3ec582f8e8df61a1c232292cd2..bdd7fd8a2a5319128c61713c43c83a31a1bb5527 100644
--- a/dist/table-widget.js
+++ b/dist/table-widget.js
@@ -134,9 +134,21 @@ export function parseCellInline(raw) {
 }
 function matchCellMarkAt(raw, from) {
     const rest = raw.slice(from);
+    // Inline code: a run of backticks, content, a matching-length run.
+    // Checked first because a code span binds tighter than emphasis in
+    // CommonMark (backticks inside `**` win), and its content is LITERAL —
+    // never parsed for nested marks (so the token carries raw text, not
+    // children). `(?!`)` stops the closer matching a longer run.
+    let m = rest.match(/^(`+)(.+?)\1(?!`)/);
+    if (m) {
+        return {
+            token: { type: 'code', delim: m[1], text: m[2] },
+            end: from + m[0].length,
+        };
+    }
     // Bold with `**` or `__` — greedy on the outside, lazy on the
     // content so we catch the nearest closer.
-    let m = rest.match(/^\*\*([\s\S]+?)\*\*/);
+    m = rest.match(/^\*\*([\s\S]+?)\*\*/);
     if (m) {
         return {
             token: { type: 'strong', delim: '**', children: parseCellInline(m[1]) },
@@ -248,6 +260,21 @@ function renderCellToken(tok) {
         wrap.appendChild(makeCellMark('~~'));
         return wrap;
     }
+    if (tok.type === 'code') {
+        // Inline code. The inner `.cm-atomic-inline-code` span reuses the
+        // outer-editor's global monospace styling; the backtick delimiters
+        // are faint marks (hidden until the caret enters the wrap). Content
+        // is a literal text node — code spans have no nested marks.
+        const wrap = document.createElement('span');
+        wrap.className = 'cm-atomic-code-wrap';
+        wrap.appendChild(makeCellMark(tok.delim));
+        const inner = document.createElement('span');
+        inner.className = 'cm-atomic-inline-code';
+        inner.textContent = tok.text;
+        wrap.appendChild(inner);
+        wrap.appendChild(makeCellMark(tok.delim));
+        return wrap;
+    }
     // Link. Shape mirrors the outer-editor markup: `.cm-atomic-link` on
     // the visible text (picks up link color + external-link icon via
     // `::after`), faint marks for `[`, `]`, `(`, URL, `)`. `data-url`
@@ -358,6 +385,7 @@ const MARK_WRAP_CLASSES = [
     'cm-atomic-strong-wrap',
     'cm-atomic-em-wrap',
     'cm-atomic-strike-wrap',
+    'cm-atomic-code-wrap',
     'cm-atomic-link-wrap',
 ];
 function isMarkWrap(el) {
